# Nothing here is required — the site builds and deploys with no env at all.

# Only used if you re-enable the auto-pulled Open Source section (see README).
# A classic token with no scopes is enough; it only reads public data.
GITHUB_TOKEN=
